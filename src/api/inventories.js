// get one inventory
export const getOneInventory = (characterId) => {
    console.log(`getOneInventory route was his for character with id ${characterId}`)
}

export const editOneInventory = (inventoryId) => {
    console.log(`editOneInventory route was hit for inventory id ${inventoryId}`)
}