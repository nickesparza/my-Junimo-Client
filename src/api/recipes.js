
// get all recipes
const getAllRecipes = () => {
    console.log('getAllRecipes route was hit')
}

// get one recipe
const getOneRecipe = (recipeId) => {
    console.log(`getOneRecipe route was hit for recipe with ID ${recipeId}`)
}